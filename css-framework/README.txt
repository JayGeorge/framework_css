# README

- Use `index.php` to prototype
- To build a component:
  - Copy `index.php` and drop it in the subfolder
  - Navigate to the subfolder (remember to include the trailing slash on the end so the stylesheet reference works) e.g. `https://css-framework.wip/2-objects/flip-card/`